[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "timeout 30 npx prisma migrate deploy; node dist/src/main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
